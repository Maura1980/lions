// Criar um programa que permita cadastrar 3 produtos, solicitando para cada um:nome, preço e quantidade em estoque.
// Ao final, exibir todos os produtos cadastrados de forma organizada no terminal

const readline = require("readline");

const rl = readline.createInterface({
    input: process.stdin,
    output: process. stdout

});
let produtos = [] 

    function cadastrarProduto() {
        if (produtos.length < 3) {
            console.log(`--Cadastro do produto ${produtos.length + 1}`);


            

            )

            )
        }


        
    }




                                 